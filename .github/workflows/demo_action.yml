name:Demo
on:
  push:
      branches:[main]

jobs:
    build:
        runs-on:windows-latest
        steps:
            -name:checkout-code
            -uses:actions/checkout@v4
            
            -name:run-command
            -run:echo "Run command executed"
            
